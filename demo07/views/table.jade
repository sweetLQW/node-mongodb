<!DOCTYPE html>
html(lang="en")
head
    meta(charset="UTF-8")
    title Document
    link(rel="stylesheet" href="style/bootstrap.min.css")
body
    table.table.table-hover
        thead
            th 姓名
            th 年龄
            th 性别
        tbody
            -for(var i = 0; i < data.length; i++)
                tr
                    td=data[i].name
                    td=data[i].age
                    td=data[i].sex
                    //each val,index in data[i]
                    //    td=val
